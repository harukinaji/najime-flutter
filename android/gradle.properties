org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.daemon=true
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# NOTE: Kotlin incremental compilation (kotlin.incremental=true) is intentionally
# OFF — on this Windows setup it crashes at the end of :record_android with
# "Could not close incremental caches (.tab)". The Flutter template disabled it
# for the same reason. Keeping it off trades per-build recompile cost for stability.
kotlin.incremental=false
kotlin.daemon.enabled=false